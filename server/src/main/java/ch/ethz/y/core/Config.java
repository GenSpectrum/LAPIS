package ch.ethz.y.core;

/**
 * The sole purpose of this interface is currently to provide the sub classes something to implement from. This makes
 * it easier to identify existing config classes.
 */
public interface Config {
}
