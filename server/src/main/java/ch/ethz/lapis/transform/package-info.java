/**
 * This package contains code to merge data from the different sources into a single table. It also transforms/
 * pre-processes the data for fast analytical queries.
 */
package ch.ethz.lapis.transform;
